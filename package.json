{
  "name": "macan-proyecto",
  "version": "1.0.0",
  "description": "Sitio web estático MACAN con servidor robusto",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "production": "pm2 start server.js --name macan --instances max --exec-mode cluster"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
